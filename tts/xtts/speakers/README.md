# XTTS reference speakers

Add one or more `.wav` files here (6–15 seconds of clear speech).

Example: `my_voice.wav` → select **XTTS — my_voice** in Settings → Audio.

Legacy location also works: `%USERPROFILE%\.ai_reading_studio\xtts_speakers\`
